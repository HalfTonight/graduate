spring:
  datasource:
    username: root
    password: 1234
    url: jdbc:mysql://localhost:3306/graduate?serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis:
  mapper-locations: classpath:mybatis/mapper/*.xml
  config-location: classpath:mybatis/mybatis-config.xml
